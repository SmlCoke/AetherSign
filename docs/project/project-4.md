
# 🚀 【集创赛·思特威杯】项目背景与系统架构全景指南 V3

> Last Updated: 2026-04-03

## 一、 赛事背景与赛题约束
*   **赛事名称：** 2026年全国大学生集成电路创新创业大赛（集创赛） - “思特威”企业命题
*   **赛道方向：** AI 芯片应用开发（端侧AI视觉应用系统）
*   **选择场景：** 赛题任务二：机器人场景（高动态场景）
*   **核心评分指标：** 极速的端到端延迟（要求应用处理帧率尽可能贴近传感器原生 90fps）、极端光照鲁棒性（亮光/纯黑）、三类系统异常（摄像头/推理/资源）的检测与恢复能力、0.8 TOPS 极低算力下的工程落地能力。

## 二、 硬件板卡资源
### 2.1 图像传感器：思特威 SC132GS

核心特性：Global Shutter（全局快门，极其适合高速运动无拖影）、单通道黑白图像（非RGB彩色）、**对红外光（IR）高度敏感**。
摄像头和开发板组合可以实现最高1280×720@90fps的采集和输出帧率，摄像头最高支持1080H×1280V@120fps的传输速率；

### 2.2 主控开发套件：飞凌微 A1 Vision 开发板

**处理器**：ARM Cortex-A7 CPU + 飞凌微自研 NPU。
**算力限制**：飞凌自研 NPU，仅有 **0.8 TOPS @ INT8** 的极轻量级算力。
**ISP算法**：片上搭载了先进的ISP算法，包括高性能暗光降噪和图像增强算法、高分辨率RGB-IR图像处理、多帧HDR合成、双路Sensor输入和图像同步处理。持双路3MP 30fps@HDR和单路3MP 60fps@HDR 支持单路5MP 60fps@RGB-IR 支持单路8MP 30fps@HDR 支持多种色彩滤波阵列(CFA)：RGGB/RGBIR/MONO
**内存带宽**：DDR3L 16bit 1Gb(Stacked)
**存储**：256Mb Nor Flash
**外设接口**：SPI、I2C、UART、GPIO等
**视频接口**：2 x 4-lane MIPI CSI RX、1 x 4-lane MIPI CSI TX，高达2.5Gbps/lane

### 2.3 NPU 算子适配性分析
A1 NPU 支持的 ONNX 算子及其限制如下：

| ONNX算子 | 限制说明 |
| --- | --- |
| Conv | Kw,Kh,Sw,Sh,Pw,Ph 均不能超过 16；单个卷积核（Kw × Kh × Cin）不能超过 2048 |
| AveragePool | 无特殊限制 |
| GlobalAveragePool | 无特殊限制 |
| MaxPool | 最大支持 8×8 |
| BatchNormalization | 无特殊限制 |
| Add | 无特殊限制 |
| Mul | 无特殊限制 |
| Concat | 支持 C通道拼接 |
| Split | 支持 C通道拆分 |
| Relu | 无特殊限制 |
| LeakyRelu | 仅支持 alpha = 0.1 或 0.01 |
| Transpose | 支持4维tensor维度重排， perm = [0,2,3,1] |
| resize	  | 支持nearest |
| convtranspose2d	| 无特殊限制 |
| Upsample	| 无特殊限制 |
    
A1 NPU 不支持或者效率极低的，但是很常见/使用频率高的算子有：Sub, Div, Transpose, Softmax，这些算子如需使用，只能在 ARM CPU 上实现。

### 2.4 图像调试软件
思特威官方提供图像调试软件 Aurora，将 A1 开发板通过 USB-Type C 连线连接笔记本电脑后，可以在 Aurora 上实时查看摄像头采集的图像效果（灰度图，90fps），并且可以显示 bounding box。

此外，该 Aurora 软件支持串口通信，可以在 Aurora 上查看开发板程序打印的信息，以及进入 Linux Shell 进行调试。

## 三、 项目定义：我们在做什么？
*   **项目名称：** 基于 SC132GS 的高帧率、抗光照干扰的、机器人场景下的高动态中文手语极速翻译系统。
*   **核心目标：** 在 0.8 TOPS 算力下，压榨 SC132GS 全局快门 90fps 的高频采样优势，实现**低延迟（<15ms）、零拖影**的高速连贯手语的手语识别(Sign Language Recognition, SLR)与手语翻译(Sign Language Translation, SLT)。结合物理红外照明，实现从白昼到黑暗环境的 **免疫光照变化** 的工业级鲁棒性。
*   **注意：** 实现的应用为实时中文手语(Chinese Sign Language, CSL)识别以及翻译，项目优先确保手语识别可做，在此基础上确保手语翻译可做.
*   **最终现场展示效果：** 人站在摄像头前做出连续手语动作，模拟聋哑人与机器人交互。摄像头捕捉动作，Aurora 调试软件上实时（接近原生90fps）将骨骼关键点显示在屏幕上。随着手语动作的进行，屏幕下侧不断输出预测出的 Gloss 序列以及最终翻译出的自然语言文本。

## 四、项目技术路线详细拆解

**暂定**的技术路线为：Sign2Gloss2Text：
1. 先完成手语动作视频 -> Gloss 序列的转化，即Sign2Gloss，这一步属于连续手语识别的范畴；
2. 在上一步基础上再将 Gloss 序列转化为自然语言文本，即 Gloss2Text，这一步属于手语翻译的范畴；
3. 如果项目进展顺利的话，后续还可以接入其他外设，例如实现文字转语音播报。

### 4.1 第一步：特征提取

这是整条链路中最关键的“视觉压缩”步骤。目标不是在端侧保留完整视频，而是**尽快**把图像压缩成**低维可解释特征。**

**物理动作：** 从摄像头的连续视频流中，逐帧提取人体和手部的关键点。
**方法**：抛弃容易受光照干扰且极其耗费算力的 RGB 全图，仅提取**双手及上半身骨骼点坐标**，实现极度降维。

如下是成熟可用并且适合我们端侧部署的工业算法：

#### (1) Google MediaPipe
- 获取：https://ai.google.dev/edge/mediapipe/solutions/guide
- 包含 **Hand Landmarker** 和 **Pose Landmarker**
- **Hand Landmarker** 同时包含手部位置检测和手部21个关键点坐标检测。在设备 Pixel 6 上的速度
为 CPU 17.12 ms。官方目前只支持 CPU 和 GPU吗，需要调整以适配 A1 NPU
- **Pose Landmarker** 输出肩、肘、腕和躯干的33个关键点坐标。类似 MobileNetV2 的 CNN
- 此外，还有 **Face Landmarker** 检测 468 个面部关键点，可以选择使用。

#### (2) RTMRose
- 获取：https://github.com/open-mmlab/mmpose/tree/main/projects/rtmpose
- 包含多个模型，包括手部21关键点检测、全身133关键点检测
- 注意：算子适配性不一定很好，Pipeline较复杂

#### (3) Yolov8n-Pose
- 获取：https://github.com/ultralytics/ultralytics/issues/1915
- 但是无法识别手部精细关键点，肩、肘、腕和躯干关键点倒是识别的不错。
- 赛题官方提供了 Yolov8n 的 A1 部署详细教程，但是没有 Yolov8n-Pose的，不过估计调整一下应该也能部署。 

#### (4) 经典 backbone 网络：RepVGG, MobileOne, 小型ResNet等
- 获取：https://github.com/DingXiaoH/RepVGG, https://github.com/apple/ml-mobileone, 以
及PyTorch官方
- 经典 backbone 网络，如果使用这些架构，会涉及到做 Teacher-Student，将关键点作为输入特征；或者直接采集原始RGB图像。所以这种方法不做优先考虑
- 优点：算子适配性很好，都是经典网络


#### 注意
1. **关键区域划分**：可以参考各种学术论文的做法，骨骼关键点划分为不同的关键区域，独立建模（或者先独立建模，后融合特征）。例如如下划分方式：
   - 左手、右手、上肢、面部
   - 左手、右手、面部、全帧
2. **部署**：上述算法模型都应该部署在 NPU 上，当然还需要做好量化和算子删减适配。

### 4.2 第二步：时序建模

**物理动作：** 观察**过去一小段时间**内骨骼点的运动轨迹，预测**每一帧**对应什么“**手语词（Gloss）**”。
**方法：**  收集连续$N$帧的骨骼点轨迹，预测对应的手语词（Gloss），如“前进”、“我”、“抓取”。由于我们做的是**实时处理并且算力有限**，所以要采取**滑动窗口**的方法，维护一个$N$帧的滑动窗口而不是连续整段视频，每隔若干帧触发一次增量推理；

如下是成熟可用并且适合我们端侧部署的工业算法：

#### (1) 时域卷积网络 TCN

- Method1: MS-TCN，获取：https://github.com/yabufarha/ms-tcn
- Method2: Causal TCN，获取：https://github.com/philipperemy/keras-tcn
- 这个阶段的网络结构比较简单，算力要求和延迟低
- 但是必须自己训练，这些网络不是专用的**坐标->手语词汇表概率矩阵**逻辑

#### 注意

1. **部署：** 可以部署在 NPU/CPU 上，但是 TCN 的算子适配性很好，并且 ARM Cortex-A7 算力有限，所以建议部署在 NPU 上。

### 4.3 第三步：Gloss 解码 / CTC 

在 Sign2Gloss 阶段，模型输出的通常不是直接文本，而是每帧 / 每时间步的 gloss logits。

**物理动作：** 把概率矩阵变成人类能看懂的句子。
**方法：** **CTC, Connectionist Temporal Classification**，一种**损失函数（Loss Function）和解码算法**。

如下是成熟可用并且适合我们端侧部署的工业算法：

#### (1) CTC

它引入了一个叫 **“Blank（空白/无意义）”** 的类别。模型在每一帧都会输出一个预测（比如："我, 我, Blank, Blank, 爱, 爱, 爱, Blank, 你"）。CTC 算法会自动把相邻重复的词合并，并删掉 Blank，最终输出干净的："我、爱、你"。它允许我们"**只提供最终的句子标签，不提供每一帧的边界**"就能完成端到端训练！

- 获取：PyTorch
- 在训练阶段，把 TCN 输出的未对齐的预测概率，和正确的手语词序列扔给它，它负责算误差并更
新 MS-TCN 的权重。
- 在板上运行阶段，CTC Greedy Decoder 完成去重和消除空白的操作，生成Gloss序列。


#### 注意
1. **部署：** CTC 建议部署在 CPU 上
2. 至此，CSLR（连续手语识别）阶段完成。如果还要做 SLT（手语翻译），就继续下面的步骤。

### 4.4 第四步：文本翻译

**物理动作：** 把手语词序列翻译为自然语言句子。
**方法：** 开发板算力有限，不能部署 Transformer Decoder 这种庞大的模型，需要部署一个能在 CPU 上运行的小型轻量级文本翻译模型/传统算法。

如下是成熟可用并且适合我们端侧部署的工业算法：

#### (1) OpenFST
- 获取：https://github.com/google-research/openfst?tab=readme-ov-file
- 构建、组合、优化和搜索 weighted finite-state transducers 的库，并明确列出应用场景包括
speech recognition、machine translation、information extraction 等
- 纯C++实现

#### (2) KenLM
- 获取：https://github.com/kpu/kenlm
- KenLM 是 C++ 编写的传统统计学 NLP 模型。它通过统计词频和概率来纠正语序，非常轻量（只需几十 MB 内存）。

#### 注意
1. **部署：** 这一步建议部署在 CPU 上，算力要求不高。此外，ARM CPU 内存极小，必须选择极轻量级模型。
2. **思考：** 一个端侧的视觉应用系统，是否真的有必要完成后端文本翻译这一步？考虑到CPU性能与延迟要求，
这一步是否一定需要做？

## 五、数据集资源

### 5.1 中文数据集
#### (1) 中文孤立词数据集

- USTC-CSL500(**重要**)：大规模数据集，500词，125, 000视频，包含 RGB、深度图 (Depth) 以及骨架
(Skeleton) 3种模态，获取途径： https://ustc-slr.github.io/datasets/2015_csl/ ，需要签署协议，
向USTC申请。该协议必须由全职员工签署（学生签署不予接受）。**目前已向USTC申请，但是至今仍未回复**
- DEVISIGN-G/D/L(**重要**)：24, 000 个视频，同样包含 RGB、深度和骨架 3 种模态。获取途径：
 https://vipl.ict.ac.cn/homepage/ksl/data.html ，申请方法同上，并且数据量较大，需要邮寄硬
盘获取
- NMFs-CSL：小规模数据集，1412视频，获取途径： https://ustcslr.github.io/datasets/2020_nmfs_csl/ ，申请方法同上
- CSSL5000：大规模数据集，100, 000视频，暂未搜索到获取链接

#### (2) 中文连续手语数据集

- CSL-Daily(**重要**)：大规模数据集，能够用于手语翻译，2000词，20, 654个视频，30fps。获取途径：
- CSL-Daily(**重要**)：大规模数据集，能够用于手语翻译，2000词，20, 654个视频，30fps。获取途径：
 https://ustc-slr.github.io/datasets/2021_csl_daily/ ，申请方法同上。**目前已向USTC申请，但是至今仍未回复**
- USTC-CCSL： 25,000 个视频，暂未搜索到获取链接
- CSL-News：大规模数据集，但主要是新闻，1985h视频。获取途径：
 https://huggingface.co/datasets/ZechengLi19/CSL-News_pose
- CE-CSL：大规模数据集，4973个训练集视频、500个test集视频（PDF中显示为513个）、516个dev集视频。训练集一共3840个词（PDF中显示为3800词）、dev集一共821个词、test集一共757个词。标注含有自然语言标注和Gloss标注，输入模态为RGB视频，没有提供关键点序列，但是已经通过Mediapipe提取好了每个视频的关键点序列。获取途径：https://github.com/woshisad159/TFNet.git . **目前主要使用该数据集（目前最高价值），优点是标注清晰且视频分辨率较高。已经保存到 Peak 本地、Quark 网盘以及 AutoDL 服务器**。该数据集的关键点个数为65，分别是pos的23个关键点，以及左右手各21各关键点，因此特征向量维度：130。


对于其他外语数据集，由于Gloss对应关系不一致，并且我们暂时不需要外语翻译，暂不做优先考虑，这里仅提供美国手语数据集连接供参考。

### 5.2 美国手语数据集

#### (1) 美国手语孤立词数据集：
- MS-ASL：1000词，25,513视频，获取途径： https://www.microsoft.com/en-us/research/project/ms-asl/ ，训练/验证/测试集已经划分好，输入是原始RGB视频，标注是孤立词。**目前已经拿到数据集的json文件，所有视频以链接形式存放（全部来源于yotube）**。但是坏链极多，很多视频失效，无法用于训练，并且视频需要从Youtube上下载，流程繁琐。
- WLASL：目前国际上最著名、使用最广泛的英文孤立词数据集之一，总共1999个词、11980个视频，分辨率较高，并且已划分train/val/test。获取途径：https://github.com/dxli94/WLASL 或者 https://www.kaggle.com/datasets/risangbaskoro/wlasl-processed 。**目前已经获取该数据集，保存在 Peak 本地以及Quark 网盘备用（目前最高价值。如果连续手语识别进展不佳，该孤立词数据集很有利用价值）**

#### (2) 美国连续手语数据集：
- RWTH-BOSTON-104：轻量级数据集(685MB)，247左右个词、训练集161条视频，测试集40条视频，分辨率195x165（RGB，Gloss标注），适合跑demo，获取途径： https://wwwi6.informatik.rwth-aachen.de/aslr/index.php ，**目前已获取该数据集，保存在 Peak 本地、Quark 网盘备用，并且尝试用Mediapipe检测关键点**。**缺点是(1) 给出的关键点数据较少，无法支持训练，需要用Mediapipe自行提取；(2) 数据量较小且分辨率极低（尺寸为195x165像素），经过测验，非常不容易检测出完整的手部位置**
- How2Sing-dwpose: How2Sign 被优化过的一个骨骼关键点子集，大规模数据集，25GB左右，15000左右个词，训练集30000个序列、验证集1700个序列、测试集2300个序列，关键点向量385维。获取途径：https://huggingface.co/datasets/FangSen9000/How2Sign-dwpose , **目前已获取该数据集，保存在 Peak 本地、Quark 网盘以及 AutoDL 服务器，并且在AutoDL服务器进行了一轮训练**。缺点是 **(1) 关键点序列部分（385维度）无意义说明，不知道哪个值对应哪个部位，作者的GitHub仓库以及Huggingface仓库均无任何说明。(2) 标注是英文自然语言句子，非 Gloss 序列，无法进行TCN + CTC 训练，因此测试效果很差，模型输出完全不匹配原始输出，参考意义不大**
- How2Sign：2500个句子，16000词，多模态。获取途径：https://how2sign.github.io/#download ，**缺点：没有 Gloss 标注，参考意义不大**
- B-F-H 2D KeyPoints: How2Sign 数据集的一个子集，包含 2D 骨骼关键点数据，80+ 小时、35000 句子量级。获取途径：https://how2sign.github.io/#download ，输入模态是关键点，自然语言标注，没有Gloss标注，无法直接参与训练，参考意义不大。**目前已获取该数据集(25GB)，保存在 Peak 本地以及Quark 网盘备用，暂未使用**


## 六、实际上板 Live Stream 多线程流水线架构规划

为了满足赛题对 **90fps 实时性**的要求，系统在 A1 开发板上采用“**异步多线程生产者-消费者架构 (Decoupled Streaming Inference)**”：
> 注意，这里只是初步构想，后续会根据实际开发情况进行调整优化。
1.  **感知线程 (Perception Thread)：** 压榨 NPU。连续读取帧，提取骨骼点，将坐标压入长度为 $N$ 的双端队列（`Deque` 滑动窗口）。
2.  **解码线程 (Translation Thread)：** 压榨 CPU。低频定时器（如每累积 $r$ 帧触发一次），从窗口取出连续数据，执行 TCN 推理 +  CTC 规则翻译 + 文本翻译。
3.  **UI 渲染线程 (Display Thread)：** 绝不阻塞。以 90fps 最高帧率读取摄像头，实时叠加渲染骨骼点连线（证明视觉鲁棒性）与底部实时滚动的翻译字幕。

## 七、 鲁棒性与三类异常处理机制

赛题要求必须有异常检测与恢复。我们的状态机监控（Watchdog）策略如下：

### 6.1 极端光照与摄像头数据异常（Data Anomaly）

**可能遇到的异常情况**：图像断流 / 遮挡 / 方差接近 0；IR 过曝；环境变暗；
解决办法待定。 

### 6.2 AI 推理异常（Inference Anomaly）

**可能遇到的异常情况**：模型输出全0或全随机值；TCN输出的Gloss序列过长过短（例如超过合理范围的10个词）；翻译结果不合理（例如过长或者过短，或者包含敏感词等）。
解决办法待定。 

### 6.3 资源异常

**可能遇到的异常情况**：温度过高；CPU/NPU 长时间高占用；帧率抖动。
解决办法待定。 

## 八、当前路线下需要特别注意的风险点

**MediaPipe 不一定适合作为板端最终运行时**
它非常适合做 PC 端 teacher / 数据标注 / demo，但板端最终量产形态更可能是自定义 tiny ONNX 网络。

**公开手语数据集与实际部署域存在差异**
大部分数据集是 RGB、30fps、实验室/电视场景；但是最终部署是 Gray/IR、90fps、机器人近场，因此必须保留自采微调计划。

**首版不要追求大词表**
对比赛来说，做出“20~50 个高质量高鲁棒命令词 + 流畅闭环”通常比做“几百上千词但板上跑不稳”更有竞争力。

## 九、目前进展、未来规划与成员分工

### 9.1 目前进展
| 阶段 | 内容 | 状态 |
|---|---|---|
| 选题方向分析 | 应用场景选择 | ✅ 完成 |
| 手语翻译资料调研 | 相关数据集、算法、论文的调研 | ✅ 完成 |
| 模型/数据研究 | 数据集申请、模型算法深入研究 | 🔲 进行中|  

### 9.2 未来规划
03-29 与指导老师讨论后，确定就沿着：Sign2Gloss2Text 这条路线来做，风险最小并且立刻就可以开始做（初赛时间逼近）。
优先确保 Sign2Gloss 能够做好，甚至优先确保骨骼关键点提取这一步能做好，能够部署在开发板上实现实时关键点定位。

04-04 与指导老师讨论后，觉得当前场景下的手语太复杂，可以先从简单指令做起，比如就做100~200的词表，每个手语句子久1~3个Gloss。

### 9.3 现阶段成员分工及进展
目前阶段：模型/数据研究
| 成员 | 分工 | 
|---|---|
| Peak | 研究关键点序列 -> Gloss 序列阶段的方法，数据集申请 |
| Dragon | 研究 MediaPipe 如何轻量化以及部署 |
| Soar | 研究 SDK 开发套件的 app_demo 代码 |

**进展**
- Peak: 已经跑通了 关键点->Gloss 序列的训练推理流程，但是目前精度较差，正在分析原因并且尝试改进。主要原因是当前数据集下同一个Gloss重复次数太少、词表数量太大而模型参数量太小。目前准备两条路线：第一条联系聋哑人手语老师自制数据集，第二条转向做孤立词手语识别。
- Dragon: MediaPipe的手掌检测与手指关键点已修改model结构，能够转为a1model。已跑通手掌检测，精度差，原因分析如下：
  - 1.采用原生MediaPipe做标注，但只检测了一个手掌。
  - 2.目前仅简单训练查看流程是否完善，数据集很小。



## 十、团队信息
* **团队名称：** PeakDragonSoar (巅峰龙翔)
* **项目名称：** AetherSign (以太印记)
* **团队成员：** 由 3 名来自 **上海交通大学 (SJTU)** 的 **2023级微电子科学与工程系** 本科生组成。
